set-attribute -A no-rc-script -V YES
set-attribute -A persistent -V YES
set-env -E NODE_ENV=production
set-env -E NEXT_TELEMETRY_DISABLED=1
mount-in -p portfolio-web -z zroot/portfolio-web -m /var/web
